# Request: add CSV column selection to the report export

Filed under "bugs" by mistake — this is a feature request, not a defect.

## Current behavior

The export always includes all 40 columns; nothing is broken.

## Desired behavior

Let the user pick which columns to include before exporting, with the current full set as the default.

## Notes

The export UI lives in app/reports/export-dialog.tsx.
